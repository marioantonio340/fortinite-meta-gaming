import React from "react";
import {  ScrollView, Text,View } from "react-native";

export const HomeScreen = () => {
    return <ScrollView>
            <Text style={{fontFamily: 'Burbank'}}>Home Screen</Text>
        </ScrollView>  
}